#include <stdlib.h>
#include "edge.h"
#include "heap.h"





void push(s_edge val, s_edge *root)
{
}



s_edge top(s_edge *root)
{
	return root[1];
}

s_edge pop(s_edge *root)
{
	s_edge mini = top(root);


	return mini;
}
