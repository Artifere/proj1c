#include <cities.h>
#include <cstdio.h>

const int incrCitiesSize = 200;


void readCoordinates(s_citiesCoordinates* citiesArray, char* fileName)
{
	FILE *file = fopen(fileName, "r");
	int nbLin = 0;
	citiesArry = malloc(incrCitiesSize * sizeof(*citiesArray));
	char read[200];
	while (read = )
}
